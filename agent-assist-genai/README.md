
# Agent Assist & GenAI — Module Folder

- `notes.md` → high-level summary
- `sessions/` → detailed notes per session
- `screenshots/` → images referenced by session notes
- `diagrams/` → architecture drawings (export as PNG/SVG)

**Linking screenshots in notes:**
```markdown
![Meaningful caption](../screenshots/01-course-introduction.png)
```
Place the image file in `screenshots/` and keep the relative path exactly as shown.
